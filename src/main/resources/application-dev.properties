# Database URL, Username and password

spring.datasource.url=jdbc:mysql://localhost:3306/jun?useSSL=false&zeroDateTimeBehavior=convertToNull
spring.datasource.username=five
spring.datasource.password=Admin22*
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server.port = 8081

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
#spring.jpa.show-sql=true

#spring.jpa.properties.hibernate.show_sql=true
#spring.jpa.properties.hibernate.use_sql_comments=true
#spring.jpa.properties.hibernate.format_sql=true

#logging.level.org.hibernate.SQL=debug
#logging.level.org.hibernate.type.descriptor.sql=trace

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
#spring.jpa.hibernate.ddl-auto=update

# Naming strategy
spring.jpa.hibernate.naming-strategy=org.hibernate.cfg.EJB3NamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.ddl-auto= update

#Permite camelCase en el nombre de tablas y campos en los entity.
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

#Database Mongo
jun.mongodb.host=localhost
jun.mongodb.port=27017
jun.mongodb.database=jun
jun.mongodb.username=
jun.mongodb.password=

#JWT
jun.app.jwtSecret= apiJunSecretKey
jun.app.jwtExpirationMs= 86400000
